---

imports:
  upstream|u_e|u_d: '.'

git module upstream:
  url: 'https://github.com/GoogleCloudPlatform/continuous-deployment-on-kubernetes.git'

rule u_e:
  export: 'sample-app'

rule u_d:
  drop:
    - 'Jenkinsfile'
    - 'k8s'

#  vim: set ft=yaml ts=4 sw=2 tw=0 et :
